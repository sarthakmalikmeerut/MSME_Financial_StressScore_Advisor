1. pip install -r requirements.txt
2. pip install pathway
3. run this in your main directory inside wsl: - export PYTHONPATH=$(pwd)
4. From the project root (same directory as main.py), run: python main.py